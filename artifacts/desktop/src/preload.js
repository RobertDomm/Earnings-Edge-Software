'use strict';

// Preload script runs in a privileged context before the page loads.
// We intentionally expose nothing — the app only loads the hosted URL
// and needs no bridge between Node and the renderer.
